 /* * Copyright 2003 Transformatorhuis, VOF. All  Rights Reserved. * * You acknowledge that Software is not designed, licensed or * intended for use in the design, construction, operation or * maintenance of any nuclear facility. */package net.transformatorhuis.cgi.conversion;import java.io.*;class Rib2Xml {    public static void main(String[] argv) {        // is there anything to do?        if (argv.length == 0) {            printUsage();            System.exit(1);        }        try {                new Rib2Xml(new File(argv[0]));        } catch (Exception thEx) {                System.out.println("File (" + argv[0] + ") not found");                System.exit(1);        }                }        public Rib2Xml(File thRibFile) {        try {            StreamTokenizer thTokens = new StreamTokenizer(new FileReader(thRibFile));            //StreamTokenizer thTokens = new StreamTokenizer(new FileInputStream(thRibFile));            thTokens.resetSyntax();            thTokens.commentChar('#');            thTokens.eolIsSignificant(true);                        try {                while(true) {                        thTokens.nextToken();                        System.out.println(thTokens.lineno() + ": " + thTokens.sval);                                }            } catch (Exception thEx) {                    System.out.println("End of file reached");            }        } catch (FileNotFoundException ex) {            System.out.println("File not found");            //throw new Exception();        }                }    private static void printUsage() {            System.err.println("usage: java net.transformatorhuis.cgi.conersion.Rib2Xml file");            }}